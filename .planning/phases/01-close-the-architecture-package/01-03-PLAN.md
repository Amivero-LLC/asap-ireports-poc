---
phase: 01-close-the-architecture-package
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - CLAUDE.md
  - README.md
autonomous: true
requirements: [ARCH-04]

must_haves:
  truths:
    - "CLAUDE.md § Current state does not assert that application code does not exist, and README.md § Status does not assert that the orchestration framework is undecided — both provably by grep, not by reading."
    - "Both files state ADR-020/ADR-021's narrowed scope: the orchestrator spine, three phases, not nine (D-12)."
    - "CLAUDE.md § Current state's inventory matches the repository after plans 01-01 and 01-02: fourteen contracts, seven handoff documents, the measured test count."
    - "CLAUDE.md § Target layout no longer reads as a build commitment to directories ADR-020 cut; it points at the build-state table as the authority (D-12's flagged contradiction)."
    - "README.md § Status records the component-architecture write-up as done and links it, and records SpecialistResult as published."
    - "The repo health baseline is unchanged — no code was touched by this plan."
  artifacts:
    - path: "CLAUDE.md"
      provides: "An entry document a fresh session can trust — current inventory, current scope, no self-contradiction"
      contains: "Three phases, not nine"
    - path: "README.md"
      provides: "A program reader's entry point that reflects a closed Milestone 1a"
      contains: "docs/handoff/component-architecture.md"
  key_links:
    - from: "CLAUDE.md"
      to: "docs/handoff/component-architecture.md"
      via: "§ Target layout and § Stack defer to the build-state table for what is built, planned, and designed-not-built"
      pattern: "component-architecture\\.md"
    - from: "README.md"
      to: "docs/handoff/component-architecture.md"
      via: "§ Status links the write-up and § Start here lists it"
      pattern: "component-architecture\\.md"
---

<objective>
Make `CLAUDE.md` § Current state and `README.md` § Status true (ARCH-04), bounded by D-12.

**Read this before planning your edits — what ARCH-04 was written against has partly already been
fixed.** ARCH-04's acceptance in `.planning/REQUIREMENTS.md`, the blocker in `.planning/STATE.md`,
and D-12 in `01-CONTEXT.md` all describe two stale claims: that `CLAUDE.md` § Current state says
application code does not exist, and that both files imply the orchestration framework is undecided.
Both were fixed in commit `4de0ad1` ("docs: ingest 12 docs into .planning/; fix four stale claims
they exposed"), which predates the discussion that produced `01-CONTEXT.md`. Verified fresh against
the working tree on 2026-08-11:

- `CLAUDE.md` § Current state is a dated inventory table listing what exists and works. It does not
  say application code does not exist.
- `CLAUDE.md` § Stack names LangGraph as the orchestration choice, with ADR-012 accepted.
- `README.md` § Status records "1c — orchestration bake-off: done. ADR-012 accepted — the framework
  is LangGraph."

**ARCH-04 is not therefore complete, and is not dropped.** ROADMAP success criterion 5 has three
clauses and only the first two are already satisfied. What remains is real:

1. Neither file states ADR-020/ADR-021's narrowed scope anywhere. No sentence in `CLAUDE.md` or
   `README.md` says there are three phases. That is criterion 5's third clause and D-12's second
   half, and it is unmet.
2. Both inventories go stale the moment plans 01-01 and 01-02 land — thirteen contracts become
   fourteen, six handoff documents become seven, the component-architecture write-up stops being
   outstanding, and the test count moves. Leaving that is the exact failure that produced ARCH-04.
3. The contradiction D-12 flags for the planner: `CLAUDE.md` § Target layout still presents
   `policy/`, `delivery/`, `workers/`, `policy-packs/`, and `evals/` as the plan, when ADR-020 cut
   all of them. That is an outright contradiction with the same file's own scope, not a mere
   omission, and `CLAUDE.md` is the first thing a fresh session reads.

**Scope discipline (D-12).** This is not a full coherence pass over `CLAUDE.md`. The fix for item 3
is one preamble sentence plus one pointer — the layout is reframed as blueprint-derived and wider
than the buildable scope, deferring to the build-state table for what is actually built. Do not
rewrite the layout listing directory by directory, do not re-derive the stack table, and do not
touch the rules sections. Choose less.

Purpose: a stale entry document is the single most expensive thing in this repository, and this one
went stale once already with nothing to catch it.

Output: two edited markdown files. No code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-close-the-architecture-package/01-CONTEXT.md
@.planning/phases/01-close-the-architecture-package/01-PATTERNS.md
@.planning/phases/01-close-the-architecture-package/01-01-SUMMARY.md
@.planning/phases/01-close-the-architecture-package/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLAUDE.md — current inventory, ADR-020 scope, and the target-layout contradiction</name>
  <files>CLAUDE.md</files>

  <read_first>
    - `CLAUDE.md` — read in full, working-tree state. Pay attention to § Current state (the
      "Last updated" line, the inventory table, the "What does **not** exist yet" line, and the
      "Outstanding before M1 sign-off" line), § Target layout (its preamble sentence and the fenced
      directory listing), and § Stack (the decided-choices table). Do not assume the file matches
      `01-CONTEXT.md`'s description of it.
    - `.planning/PROJECT.md` — the scope line reading "Scope is the orchestrator spine (ADR-020).
      Three phases, not nine." That phrasing is reused verbatim so the two documents agree word for
      word.
    - `.planning/ROADMAP.md` — the spine paragraph and the three phase headings.
    - `docs/DECISIONS.md` §ADR-020 and §ADR-021 — what was cut, what was explicitly retained.
    - `docs/handoff/component-architecture.md` — written in plan 01-02, so the pointer you add names
      a real file and a real section.
    - `.planning/phases/01-close-the-architecture-package/01-01-SUMMARY.md` and `01-02-SUMMARY.md` —
      the actual contract count and test-count delta.
    - Before writing any number, run `uv run pytest -q` and `ls docs/handoff/*.md`. Read the counts
      off the output; do not carry them over from this plan or from a summary.
  </read_first>

  <action>
Make four targeted edits to `CLAUDE.md`. Change nothing else.

**1. § Current state — the inventory table.** Update three rows to the measured post-Phase-1 state.
The `packages/domain/` row changes from thirteen contracts to fourteen and names `SpecialistResult`
as the addition. The `docs/handoff/` row changes from six handoff documents to seven and names the
component-architecture write-up as the seventh. The `Tests` row carries the actual passed and
skipped counts from `uv run pytest -q`, keeping the existing note that the skips are live-model
tests opt-in via `IREPORTS_LIVE_SMOKE=1`. Update the "Last updated" date to the date of the edit.

**2. § Current state — the outstanding line.** The sentence beginning "Outstanding before M1
sign-off: the component-architecture write-up (the last 1a item), and cold start under SAM local"
is false once plan 01-02 lands. Rewrite it: Milestone 1a is closed; what remains outstanding is cold
start and packaging under SAM local, which is unmeasured, has no scheduled phase because ARCH-03 was
cut by ADR-020, and is the one number that could reopen ADR-012. Keep the existing
`spikes/test_scorecard.py` framing if the sentence carries it — the visible-gap mechanism is the
point.

**3. § Current state — the ADR-020/ADR-021 scope statement.** This is D-12's second half and ROADMAP
criterion 5's third clause, and nothing in `CLAUDE.md` currently says it. Add a short paragraph
immediately after the "What does not exist yet" line stating: the buildable scope is the orchestrator
spine (ADR-020), reusing `.planning/PROJECT.md`'s wording verbatim — "Three phases, not nine";
eighteen requirements moved to `.planning/REQUIREMENTS.md` § v2 with their acceptance intact and
nothing was deleted; ADR-021 restored retrieval to the spine while RETR-03 and CONT-02 stay cut; and
ADR-011 (the human disposition gate) and ADR-014 (no aggregate score) were considered for the cut and
explicitly kept, both still NON-NEGOTIABLE. Add one sentence saying the per-component account of what
is built, planned, and designed-not-built lives in `docs/handoff/component-architecture.md` and is
enforced by `tests/architecture/test_build_state_table.py`.

**4. § Target layout and § Stack — the D-12 contradiction.** Two small edits and no more.

In § Target layout, replace the preamble sentence that currently reads "Adapted from `blueprint.md`
§5.2, trimmed to the decisions in `docs/DECISIONS.md` (no `ui/`, no Neo4j, no offline fixture
profile):" with a preamble that keeps the same listing but stops it reading as a build commitment.
It must say that this is the blueprint-derived target layout; that it is wider than the buildable
scope, since ADR-020 and ADR-021 pared the build to the orchestrator spine; that several directories
below — `policy/`, `delivery/`, `workers/`, `policy-packs/`, and `evals/` among them — are designed
and not built; and that the authority on what is built, planned, and designed-not-built is the
build-state table in `docs/handoff/component-architecture.md`. Do not edit the fenced listing itself
and do not re-derive the status of each directory here; that is the full coherence pass D-12
excludes.

In § Stack, add one sentence directly beneath the decided-choices table saying the table records
which technology was chosen for each layer, not which layer has been built, and pointing at the same
build-state table. Do not change any row of the table.
  </action>

  <verify>
    <automated>uv run pytest -q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -ci "three phases, not nine" CLAUDE.md` returns at least `1`, and the matched wording is
      identical to the phrasing in `.planning/PROJECT.md`.
    - `grep -c "component-architecture.md" CLAUDE.md` returns at least `3` (the scope paragraph,
      § Target layout, § Stack).
    - `CLAUDE.md` contains `tests/architecture/test_build_state_table.py`, `ADR-020`, `ADR-021`,
      `ADR-011`, `ADR-014`, `ARCH-03`, and `RETR-03`.
    - `grep -i "application code does not exist" CLAUDE.md` returns nothing.
    - `grep -i "13 Pydantic v2 contracts\|Six handoff documents" CLAUDE.md` returns nothing.
    - `CLAUDE.md` § Current state's `Tests` row states counts equal to the passed and skipped counts
      printed by `uv run pytest -q`.
    - `grep -c "Adapted from \`blueprint.md\` §5.2, trimmed to the decisions" CLAUDE.md` returns `0`
      (the preamble was replaced).
    - The fenced directory listing under § Target layout is byte-identical to its pre-edit content:
      `git diff CLAUDE.md` shows no change to any line inside that fence.
    - No row of the § Stack table changed: `git diff CLAUDE.md` shows only added lines beneath it.
    - `git diff --stat --exit-code -- packages tests schemas scripts pyproject.toml` exits 0 — this
      plan touches no code.
    - `uv run pytest -q` exits 0 with the same passed and skipped counts as at the end of plan 01-02.
  </acceptance_criteria>

  <done>
`CLAUDE.md`'s inventory matches the repository, its scope paragraph states ADR-020/ADR-021's three
phases, and § Target layout and § Stack defer to the build-state table instead of contradicting the
project's own scope.
  </done>
</task>

<task type="auto">
  <name>Task 2: README.md — a closed Milestone 1a and the narrowed scope</name>
  <files>README.md</files>

  <read_first>
    - `README.md` — read in full, working-tree state. Pay attention to § Status (the three milestone
      bullets and the fenced bash block whose comment reads `# 111 passed, 8 skipped`), § Start here
      (the document table), and § Stack (decided).
    - `docs/handoff/component-architecture.md` — for the link target and to describe it in one line.
    - `docs/handoff/contracts.md` — as edited by plan 01-01, for the contract count and the
      `SpecialistResult` description.
    - `.planning/PROJECT.md` — the scope wording reused verbatim.
    - `.planning/phases/01-close-the-architecture-package/01-01-SUMMARY.md` and `01-02-SUMMARY.md`.
    - Before writing any number, run `uv run pytest -q` and read the counts off the output.
  </read_first>

  <action>
Make four targeted edits to `README.md`. Change nothing else.

**1. § Status — the 1a bullet.** It currently reads "1a — contracts: done. Thirteen data contracts
as Pydantic v2 models with generated JSON Schema, in `packages/domain/`. The component-architecture
write-up is still outstanding." Both halves are now false. Rewrite it as 1a done and closed:
fourteen data contracts, naming `SpecialistResult` as the addition and describing it in a clause as
the typed return value of one specialist sub-call; and the component-architecture write-up complete,
linked as `[docs/handoff/component-architecture.md](docs/handoff/component-architecture.md)`,
following the markdown link style the 1c bullet already uses. Change the milestone framing line
"Milestone 1 in progress." to state that Milestone 1 is complete, if the three bullets support that
after plans 01-01 and 01-02 land — check them rather than assuming.

**2. § Status — the scope paragraph.** Add a short paragraph after the three bullets stating
ADR-020/ADR-021's narrowed scope, reusing `.planning/PROJECT.md`'s wording verbatim: the scope is
the orchestrator spine (ADR-020), three phases, not nine. Say that nothing was deleted — sixteen
requirements moved to v2 with their acceptance intact — and that the account of what was designed
and deliberately not built, with the reason for each, is in
`docs/handoff/component-architecture.md`. A program reader who sees a narrower build without seeing
that sentence will read it as a project that shrank rather than a project that chose.

**3. § Status — the bash block.** Update the `# 111 passed, 8 skipped` comment to the counts printed
by `uv run pytest -q` at the time of the edit. Leave the commands themselves unchanged.

**4. § Start here — the document table.** Add a row for
`[docs/handoff/component-architecture.md](docs/handoff/component-architecture.md)` describing it as
the component boundaries, what is ours, what AWS ingestion and ASAP own, and what was designed and
not built. Place it above the `docs/handoff/contracts.md` row, since a reader wants the boundaries
before the contracts.

Do not touch § Decision-support boundary, § The central question, § Stack (decided), or § Data.
`README.md` § Stack already says "any UI in Milestone 1" and names LangGraph correctly; leave it.
  </action>

  <verify>
    <automated>uv run pytest -q</automated>
    <human-check>Read `README.md` § Status and § Start here as a member of the ASAP program team who has never seen this repository. Confirm you learn, in that order: Milestone 1 is closed, the scope is three phases not nine, nothing was deleted, and there is one document that tells you what was and was not built. If any of those four takes a second read, the section is not done.</human-check>
  </verify>

  <acceptance_criteria>
    - `grep -c "component-architecture.md" README.md` returns at least `3` (the 1a bullet, the scope
      paragraph, the § Start here row).
    - `grep -i "still outstanding" README.md` returns nothing.
    - `grep -i "Thirteen data contracts" README.md` returns nothing; `README.md` contains
      `Fourteen data contracts` and `SpecialistResult`.
    - `grep -ci "three phases, not nine" README.md` returns at least `1`, matching
      `.planning/PROJECT.md`'s wording.
    - `README.md` contains `ADR-020`.
    - The bash block's comment states counts equal to the passed and skipped counts printed by
      `uv run pytest -q`.
    - `README.md` § Start here table contains a row whose link target is
      `docs/handoff/component-architecture.md`, positioned above the `docs/handoff/contracts.md` row.
    - `git diff README.md` shows no changes to the § Decision-support boundary, § The central
      question, § Stack (decided), or § Data sections.
    - Every markdown link in `README.md` resolves to a file that exists — check each relative link
      target with `ls`.
    - `git diff --stat --exit-code -- packages tests schemas scripts pyproject.toml` exits 0.
  </acceptance_criteria>

  <done>
`README.md` § Status shows a closed Milestone 1a with fourteen contracts and the write-up linked,
states ADR-020's three-phase scope, carries the measured test counts, and § Start here points a
program reader at the boundaries document first.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repository → the ASAP program team | `README.md` is the first document an external reader opens. What it claims about what exists is read as authoritative by a team that cannot easily check our work (ADR-001). |
| this repository → a future Claude session | `CLAUDE.md` is loaded into every session's context and its claims are acted on without verification. A false statement there propagates into code. |

No network surface, no user input, no authentication, no data store, and no code is touched by this
plan.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-15 | Tampering / misrepresentation | `CLAUDE.md` § Current state | mitigate | An overstated inventory is how a reader — human or agent — builds on something that does not exist. This already happened here, which is why ARCH-04 exists. Mitigation is that every number in this task is read off a command (`uv run pytest -q`, `ls docs/handoff/*.md`) rather than carried over from a plan or summary, and the acceptance criteria assert equality with that command's output. |
| T-01-16 | Tampering / misrepresentation | `README.md` § Status | mitigate | Claiming Milestone 1 complete without the write-up and the contract actually landing would be exactly ADR-001's failure mode. Mitigated by wave ordering — this plan runs after 01-01 and 01-02 — and by acceptance criteria that require the linked file to exist and every relative link to resolve. |
| T-01-17 | Information Disclosure | both files | mitigate | Entry documents are read widely, so they are a plausible place for deployment detail or case data to leak. Neither task adds an example, an identifier, a credential, an endpoint, or a model id — the edits are counts, one scope paragraph, one preamble, and one table row. `CLAUDE.md`'s existing synthetic-data-only and no-hard-coded-model-id rules are untouched. |
| T-01-18 | Elevation of Privilege | § Target layout preamble | mitigate | The reframing must not read as permission to build the cut directories, and must not read as a claim that they were never designed. It defers to the build-state table, which carries a `DESIGNED-NOT-BUILT` row and a reason for each — so the pointer resolves to an honest account rather than to silence. |
| T-01-19 | Denial of Service | — | n/a | No runtime component, no service, no resource under any external party's control. |
| T-01-20 | Spoofing / Repudiation | — | n/a | No identity, authentication, or audit boundary exists in this plan. Provenance for the edits is the git commit itself. |
| T-01-SC | Tampering | package installs | n/a | This plan installs no packages, changes no lockfile, and touches no code. Two acceptance criteria assert `git diff --stat --exit-code` is clean over `packages`, `tests`, `schemas`, `scripts`, and `pyproject.toml`. |

**ASVS L2 scoring:** No ASVS chapter has live surface in this plan — it edits two markdown files and
runs the existing test suite to read a number. V2, V3, V4, V5, V6, V7, V8, V9, V11, V12, V13, V14
are all not applicable for that reason. No `high`-severity finding is possible; the repo health
baseline must be byte-for-byte unchanged on the code side.
</threat_model>

<verification>
Run from the repo root:

- `uv run pytest -q` — exits 0, with the same passed and skipped counts as at the end of plan 01-02.
- `git diff --stat --exit-code -- packages tests schemas scripts pyproject.toml` — exits 0.
- `uv run ruff check` and `uv run mypy --strict packages tests` — exit 0, unchanged.
- `grep -i "application code does not exist" CLAUDE.md README.md` — returns nothing.
- `grep -i "framework is undecided\|orchestration framework has not been" CLAUDE.md README.md` —
  returns nothing.
- `grep -ci "three phases, not nine" CLAUDE.md README.md` — returns at least 1 for each file.
- Every relative markdown link in `README.md` resolves.
- Human check: the § Status read-through described in Task 2.
</verification>

<success_criteria>
ROADMAP Phase 1 success criterion 5 is TRUE and provable: `CLAUDE.md` § Current state and
`README.md` § Status no longer assert that application code does not exist or that the orchestration
framework is undecided — verified by grep rather than by reading — and both reflect ADR-020's scope
in `.planning/PROJECT.md`'s own words (D-12).

In addition:
- Both inventories match the repository as it stands after plans 01-01 and 01-02, with every count
  read off a command.
- `CLAUDE.md` no longer contradicts itself about what is being built; § Target layout and § Stack
  defer to the build-state table (D-12's flagged contradiction, fixed minimally).
- No code, test, schema, or dependency changed.
</success_criteria>

<output>
Create `.planning/phases/01-close-the-architecture-package/01-03-SUMMARY.md` when done.
Record: the exact counts written into each file and the command each was read from; the replacement
text used for the § Target layout preamble; and — importantly for the phase record — restate the
finding that `4de0ad1` had already fixed two of the three staleness claims ARCH-04 was written
against, so `.planning/REQUIREMENTS.md` ARCH-04 and `.planning/STATE.md`'s blocker list can be
closed accurately rather than as though all three were done here.
</output>
